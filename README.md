# Car Dealership SQLite (C)

Career-grade, runnable SQLite project derived from COMP 3150 coursework.

## Build
```bash
mkdir build
cd build
cmake ..
cmake --build .
```

## Run
```bash
./car_db
```

The program auto-creates `car.db`, inserts demo data, and prints query results.
